import React from 'react';
import { Text, TextInput, View } from 'react-native';

const SearchProjectsScreen = props => {
  return (
    <View>
      <TextInput placeholder='Search projects' />
    </View>
  );
};

export default SearchProjectsScreen;
