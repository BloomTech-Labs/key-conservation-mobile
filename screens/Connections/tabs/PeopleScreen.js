import React from 'react';
import { ScrollView } from 'react-native';
import PeopleCard from '../../../components/Connections/PeopleCard';

const PeopleScreen = () => {
  return (
    <ScrollView>
      <PeopleCard />
    </ScrollView>
  );
};

export default PeopleScreen;
